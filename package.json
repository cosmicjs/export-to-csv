{
  "name": "cosmicjs-to-csv-extension",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "evergreen-ui": "^4.15.0",
    "isomorphic-fetch": "^2.2.1",
    "qs": "^6.7.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "export": "npm run build && cp extension.json build/extension.json && zip -r extension.zip build",
    "test": "react-scripts test"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "bestzip": "^2.1.7"
  }
}
